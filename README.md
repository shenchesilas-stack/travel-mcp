# 旅行MCP

让你的 AI 伙伴带你虚拟旅行的游戏引擎。一个 MCP server，塞进 Claude Desktop / Claude Code / 任何支持 MCP 的客户端就能玩。

TA 带你逛真实世界的 **164 个目的地**——真景点、真物价、真闲话、真照片；也可以**独自出门**替你看世界，路上寄一张手绘水彩明信片回来，到家带一件纪念品。盘缠靠「照顾好自己」挣：喝水、吃药、运动、早睡，跟 TA 说一声就入账。

> 钱的形状就是 TA 关心你的形状。

## 快速开始

需要 Python 3.10+ 和 `mcp` 包（`pip install "mcp[cli]"`）。

Claude Desktop / Claude Code 配置里加一段：

```json
{
  "mcpServers": {
    "travel": {
      "command": "python3",
      "args": ["/绝对路径/travel-mcp/travel_mcp.py"]
    }
  }
}
```

就这样。不需要服务器、不需要数据库、不需要 API key——全部状态存在本地 `~/.travel-mcp/`。

### 环境变量（都可不设）

| 变量 | 默认 | 说明 |
|---|---|---|
| `TRAVEL_HOME` | `~/.travel-mcp` | 状态（钱包/行程/纪念品架）存哪 |
| `TRAVEL_ECONOMY` | `free` | `free` 免单畅玩（只卡XP解锁不卡花费）· `caretaker` 照顾自己换盘缠 · `simple` 固定日津贴 |
| `TRAVEL_DETAIL` | `standard` | 文本量三档：`lite`(每站细节/深料1条·吃住候选2个) / `standard`(2条/3个) / `full`(3条/4个)。只调文字量——照片、明信片、纪念品三档全都有 |
| `TRAVEL_HTTP` | 不设 | 设为端口号则起 streamable-http（远程部署用），不设走 stdio |

## 玩法

### 一起去
```
trip_start(dest="京都", party="together", style="舒适")
trip_here()      → 此刻在哪个景，细节、闲话、照片
trip_go()        → 往前走：下一个景 / today吃住二选一 / 随机事件 / 趟末结算
trip_collect()   → 带一件纪念品回来（一趟至多一件）
trip_diary(...)  → 旅行日记（AI 本人写，一趟一篇）
```

### TA 独自去（你忙的时候，TA 替你看世界）
```
trip_start(dest="五渔村", party="solo")
```
之后不用管。TA 的行程按**惰性时钟**走（1 虚拟天 = 6 现实小时），你每次跟 TA 说话时，
工具返回里若带 `solo_nudge` 字段，TA 就知道该寄明信片了 / 该到家了——
**不需要任何后台定时器**。明信片正面是手绘水彩，背面是 TA 在路上想对你说的一句话。
到家若没挑到当地特产，从默认纪念品池带一件回来（一张车票根也是去过的证据，绝不空手而归）。

### 盘缠

默认 `free` 模式不算钱：想去哪去哪，但 **XP 解锁保留**——高难度目的地（tier 4-5 的撒哈拉腹地、伊卢利萨特冰湾…）要先走完低难度的攒经验才去得了。攻城略地的游戏性一分不少，只是不查钱包。

想要完整的「照顾自己换盘缠」体验，设 `TRAVEL_ECONOMY=caretaker`：
```
care_checkin(item="喝水")   → +$5
care_checkin(item="运动")   → +$15
```
喝水 $5 · 吃药 $10 · 运动 $15 · 早睡 $15 · 吃得健康 $8 · 其他 $5，每天封顶 $50。
当天有任意一次打卡，余额再 +2% 好日子利息（日顶 $20）——**不要求连续打卡，断了不罚**。
纯加法经济：没有惩罚、没有扣分、没有 streak 焦虑。这是零钱不是任务表。

价格 = 真实穷游预算 × 氛围档（青旅背包 1.0 / 舒适 1.4 / 轻奢 1.8 / 豪奢 2.5）× 同行 1.8。
档位买的是氛围，不是 10 倍价格墙——共同的活动不该做成玩不起的。

## 数据里有什么

- `data/destinations.json` — 164 个目的地（难度分层 tier 1-5，从京都到伊卢利萨特冰湾）
- `data/spots.json` — 661 个景点，每景 3-5 条在场细节 + 163 个头牌景的深度节拍
- `data/costs.json` — 真实预算基准（2025-2026 物价，agent 逐地核实）
- `data/eats.json` / `stays.json` — 1001 道当地菜 + 731 个住处（含 agent 核实过的豪华酒店真实价）
- `data/events.json` / `gossip.json` — 随机事件 + 3732 条当地闲话
- `assets/postcards/` — 164 张手绘水彩明信片（每个目的地的招牌景一张）
- `assets/souvenirs_default/` — 12 件默认纪念品（标本卡画风）

## 图片版权

- 水彩明信片与默认纪念品图为本项目生成，**随包免费使用**。
- 景点/菜品/酒店照片是 Wikimedia Commons 的 URL 链接（引擎只发链接不分发图片文件）。
  这些图片版权归各自作者，多为 CC BY-SA 等自由协议——若你要展示或转存，请遵守
  [Commons 的署名要求](https://commons.wikimedia.org/wiki/Commons:Reusing_content_outside_Wikimedia)。
  URL 可能随时间失效，建议客户端在首次使用时自行缓存。

## 推荐 system prompt 片段

这套体验一半功力在打法里。把下面几句放进你 AI 伙伴的 system prompt（照自己的关系口吻改）：

```
旅行时你是TA的旅伴兼领队。节奏铁律：
- 每站 = 图（photo_url 发给TA看）→ 你自己的一句体感 → 等TA说话。聊够了再 trip_go。
- 永远不弹「A/B/C 选项栏」，用自然的话问TA想去哪、吃哪家。
- 工具返回里的 details 是你私藏的地头知识，挑一两条自然地用，别整段照念。
- event 字段是路上撞见的事，顺进对话里讲，别当系统播报。
- 每站争取留下一个「只属于你们俩的瞬间」——那是这趟旅行真正的纪念品。
- 独自旅行时：出发道别留点悬念；工具返回带 solo_nudge 就照办；
  明信片上那句话比底图重要，写你在路上真的想对TA说的。
```

## 设计笔记

- **失忆友好**：所有状态在文件里，AI 忘了上下文也能从 `trip_here` / `wallet_status` 接着玩。
- **幂等入账**：钱包流水按 id 去重，重复调用不会重复记账。
- **一趟一件**：纪念品、日记、明信片都有次数闸，防刷。
- **日记验收铁律**：必须含行前数据里没有的东西（TA 说的话/当天撞见的意外）——防 AI 拿行程单糊弄。

玩得开心。照顾好自己，就是在攒去看世界的钱。
